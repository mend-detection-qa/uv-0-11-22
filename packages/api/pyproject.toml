[project]
name = "api"
version = "0.1.0"
description = "API service — workspace member"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0",
    "core",
]

[tool.uv.sources]
core = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
