[project]
name = "core"
version = "0.1.0"
description = "Core library — workspace member"
requires-python = ">=3.11"
dependencies = [
    "click>=8.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
