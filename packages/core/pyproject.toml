[project]
name = "probe-core"
version = "0.1.0"
description = "Workspace member: core library (no workspace-root groups)"
requires-python = ">=3.11"
dependencies = [
    "click>=8.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
